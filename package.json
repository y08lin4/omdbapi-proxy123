{
  "name": "omdbapi-proxy123",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OMDb API 代理管理器，包含 Go 版和 Cloudflare Worker 版。",
  "scripts": {
    "test": "cd cf && npm test",
    "cf:dev": "npx wrangler@latest dev",
    "cf:deploy": "npx wrangler@latest deploy",
    "cf:dry-run": "npx wrangler@latest deploy --dry-run"
  },
  "engines": {
    "node": ">=20"
  },
  "cloudflare": {
    "bindings": {
      "CLIENT_KEYS": {
        "description": "你发给调用方的服务访问 key；支持一行一个、逗号或空格分隔。"
      },
      "OMDB_KEYS": {
        "description": "OMDb 官方 key 池；支持一行一个、逗号或空格分隔。不要提交到 GitHub。"
      },
      "ADMIN_KEY": {
        "description": "管理接口 key，用于 /admin/stats 和 /admin/reload。"
      }
    }
  },
  "license": "MIT"
}
